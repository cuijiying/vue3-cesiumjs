# 无人机轨迹回放功能

## 功能介绍

这是一个基于 Vue3 + CesiumJS 实现的完整无人机轨迹回放系统。支持三种预设的飞行轨迹模式，并提供完整的播放控制功能。

## 主要功能

### 1. 三种飞行轨迹模式

- **矩形巡航**: 无人机在城市上空按矩形路径巡航
- **螺旋上升**: 无人机以螺旋方式逐渐上升
- **地形跟随**: 无人机低空地形跟随飞行

### 2. 播放控制

- ▶ **播放**: 开始播放轨迹
- ⏸ **暂停**: 暂停当前播放
- ▶ **继续**: 从暂停位置继续播放
- ⏹ **停止**: 停止播放并保持当前位置
- ↻ **重置**: 重置到起点

### 3. 进度控制

- 实时显示播放进度（百分比）
- 可拖动进度条快速定位
- 显示当前时间和总时长

### 4. 速度控制

支持多种播放速度：
- 0.5x（慢速）
- 1x（正常）
- 2x（快速）
- 5x（更快）
- 10x（最快）

### 5. 视角控制

- 🎯 **跟随无人机**: 相机自动跟随无人机移动
- ✈️ **飞到无人机**: 相机飞到无人机附近观察

### 6. 实时状态显示

显示无人机的实时飞行参数：
- 经度/纬度
- 高度（米）
- 速度（米/秒）
- 航向角（度）
- 俯仰角（度）

## 使用方法

### 启动项目

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev
```

### 访问功能

1. 打开浏览器访问项目首页
2. 点击"无人机轨迹回放"卡片
3. 等待 Cesium 地图加载完成
4. 在右侧控制面板中：
   - 选择想要查看的轨迹类型
   - 点击"播放"按钮开始回放
   - 使用其他控制按钮调整播放状态

## 项目结构

```
src/
├── components/
│   └── DroneControlPanel.vue          # 控制面板组件
├── composables/
│   ├── useCesiumViewer.ts             # Cesium 初始化
│   └── useDronePlayback.ts            # 轨迹回放逻辑
├── data/
│   └── droneTrajectory.ts             # 轨迹数据生成
├── views/
│   ├── HomeView.vue                   # 首页
│   └── DronePlaybackView.vue          # 回放页面
└── router/
    └── index.ts                       # 路由配置
```

## 技术实现

### 轨迹数据生成

- 使用算法生成模拟的飞行轨迹数据
- 每个轨迹点包含：时间、位置（经纬度、高度）、姿态（航向、俯仰、翻滚）、速度
- 支持在轨迹点之间进行插值，生成平滑的飞行路径

### 轨迹可视化

- 使用 Cesium 的 Entity API 创建无人机模型（圆锥体）
- 使用发光多段线显示完整飞行轨迹
- 动态更新无人机的位置和姿态

### 播放控制

- 基于 `requestAnimationFrame` 实现平滑动画
- 支持实时调整播放速度
- 支持进度条拖动快速定位

### 相机控制

- 提供跟随模式和独立观察模式
- 平滑的相机过渡动画

## 自定义轨迹数据

如果想要添加自己的轨迹数据，可以在 `src/data/droneTrajectory.ts` 中：

1. 创建新的轨迹生成函数
2. 在 `trajectoryPresets` 中添加新的预设项
3. 在控制面板中添加对应的选项

轨迹数据格式：

```typescript
interface DronePosition {
  time: number        // Unix 时间戳（秒）
  longitude: number   // 经度
  latitude: number    // 纬度
  altitude: number    // 高度（米）
  heading: number     // 航向角（度）
  pitch: number       // 俯仰角（度）
  roll: number        // 翻滚角（度）
  speed: number       // 速度（米/秒）
}
```

## 注意事项

1. 确保已安装 Cesium 相关依赖
2. 如需使用 Cesium Ion 的高级功能，需要配置访问令牌
3. 建议使用现代浏览器（Chrome、Firefox、Edge 最新版）以获得最佳性能
4. 轨迹数据量较大时可能影响性能，建议适当调整插值点数量

## 后续优化方向

- [ ] 支持导入实际的飞行日志文件（如 KML、GPX 格式）
- [ ] 添加更多无人机模型（glTF 格式）
- [ ] 支持多架无人机同时回放
- [ ] 添加轨迹编辑功能
- [ ] 导出轨迹数据和回放视频
- [ ] 添加地形高度数据实现真实的地形跟随
- [ ] 优化大数据量轨迹的渲染性能
